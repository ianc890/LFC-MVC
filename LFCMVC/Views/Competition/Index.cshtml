@model IEnumerable<LFCMVC.Models.Competition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img alt="" src="~/Images/@Html.DisplayFor(modelItem => item.Image)" width="100" height="100">
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CompetitionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CompetitionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CompetitionId })
        </td>
    </tr>
}

</table>
